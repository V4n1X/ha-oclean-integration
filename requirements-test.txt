# Test/lint tooling for the oclean_ble custom integration.
#
# The target platform is Home Assistant 2026.9.1 (released 2026-09-05), which
# requires Python >= 3.14.2.  The unit tests themselves are hermetic: they stub
# every homeassistant.* module (see tests/conftest.py) and therefore also run on
# older interpreters.  The homeassistant pin is applied whenever a suitable
# interpreter is available so that CI validates against the real target version.
homeassistant==2026.9.1; python_version >= "3.14"

pytest>=9.1.0
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0
coverage>=7.15.0
voluptuous>=0.16.0
ruff>=0.16.3
mypy>=2.1.0
